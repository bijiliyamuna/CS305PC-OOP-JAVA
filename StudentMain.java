// java.io + unit 1 topics
package Student;
import java.util.Scanner;
import java.io.*;
class StudentMain implements Operations{
	static id,name,branch,password;
	Scanner s = new Scanner(System.in);
	public void logIn(String id,String password){
		try{
		BufferedReader reader = new BufferedReader(new Filename(frame));
		String data;
		while(data=reader.readLine()){
		String 
		}
		}
	}


}
